# Using simplified requirements now - see requirements-simple.txt
# This file is kept for reference but not used in deployment

# Core dependencies with versions that work on Render
fastapi==0.104.1
uvicorn==0.24.0
python-multipart==0.0.6
sqlalchemy==2.0.23
python-dotenv==1.0.0

# Fix numpy version that works with Render
numpy==1.24.3

# Google AI
google-generativeai==0.3.2

# Use requirements-simple.txt for actual deployment
