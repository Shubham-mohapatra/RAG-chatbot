# Core dependencies for backend only
fastapi==0.104.1
uvicorn==0.24.0
python-multipart==0.0.6
sqlalchemy==2.0.23
google-generativeai==0.3.2
python-dotenv==1.0.0

# Use older pydantic to avoid Rust compilation
pydantic==1.10.8

# LangChain without native dependencies
langchain==0.0.352
langchain-core==0.1.3
langchain-google-genai==0.0.6
langchain-community==0.0.16

# ChromaDB with specific version that has pre-built wheels
chromadb==0.4.6

# Supporting packages
sentence-transformers==2.2.2
